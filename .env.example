# Copy this file to .env and fill in the values.

# Any random string, used to sign the login session cookie.
SESSION_SECRET=replace-with-a-random-string

# Get a free key at https://openrouter.ai (API Keys section).
# Only needed for the AI chat feature; the board works without it.
OPENROUTER_API_KEY=
